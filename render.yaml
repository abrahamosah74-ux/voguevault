services:
  - type: web
    name: voguevault-api-gateway
    env: node
    buildCommand: cd backend && npm install && npm run build
    startCommand: node backend/services/api-gateway/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
